{
    "compilerOptions": {
      "jsx": "react",
      "baseUrl": ".",  // Makes the project root the base path
      "paths": {
        "@/*": ["src/*"] // Alias "@" to the "src" folder
      }
    },
    "include": ["src"],  // Include all files in the "src" directory
    "exclude": ["node_modules"] // Exclude dependencies
  }
  